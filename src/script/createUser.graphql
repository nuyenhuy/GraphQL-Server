mutation {
    createUser(userInput: {
        name: "John Doe"
        email: "johndoe@gmail.com"
        password: "securepassword"
        role: "ROLE_ADMIN"}) {
        id
        name
        email
        role
    }
}

